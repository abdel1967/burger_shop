{% extends 'base.html.twig' %}

{% block title %}{{ product.name }}-BurgerShop{% endblock %}

{% block content %}

        <div class="row" id="bip">
            <div class="col-md-5">
                <img src="/uploads/{{ product.illustration }}" alt="{{ product.name }}" class="img-fluid bg-secondary">
            </div>
            <div class="col-md-7 my-auto">
                <h3>{{ product.name }}</h3>
                <p><strong>{{ product.subtitle }}</strong></p>
                <span class="product-page-price">{{ (product.price /100)|number_format(2, ',', '.') }} €</span>
                 <p>
                {{ product.description }}
                </p>
                <a href="{{ path('add-to-cart', {'id' : product.id}) }}" class="btn btn-info " ><strong>Ajouter au panier</strong></a>
            </div>
        </div>
            <hr>
        <div class="move">
                    <h2>Nos meilleurs ventes</h2>
                    <p>Nos produits les plus vendu </p>
                        <div class="row">
                            {% for product in products %}
                                <div class="col-md-3">
                                    {% include 'product/single_product.html.twig' %}
                                </div>
                            {% endfor %}
                        </div><!-- /.row -->
        </div>
{% endblock %}
